spring:
  application:
    name: queue-edu

rabbit:
  properties:
    port: 5672
    host: localhost
    username: override-guest
    password: override-password
    virtualHost: /
    queueName: com.4rl1996.queue